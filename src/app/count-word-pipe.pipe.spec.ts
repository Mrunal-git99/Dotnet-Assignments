import { CountWordPipePipe } from './count-word-pipe.pipe';

describe('CountWordPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountWordPipePipe();
    expect(pipe).toBeTruthy();
  });
});
