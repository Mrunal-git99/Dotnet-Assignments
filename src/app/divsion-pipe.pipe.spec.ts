import { DivsionPipePipe } from './divsion-pipe.pipe';

describe('DivsionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DivsionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
