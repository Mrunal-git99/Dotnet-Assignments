import { RepeatWordsPipePipe } from './repeat-words-pipe.pipe';

describe('RepeatWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
